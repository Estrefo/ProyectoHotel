@page "/AccessDenied"
@attribute [StreamRendering(true)]
@rendermode @(new InteractiveServerRenderMode(prerender: false))


@inject NavigationManager NavigationManager

<style>
    .access-denied-container {
        margin-top: 120px; /* Espacio superior */
        display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        .access-denied-container h1 {
            font-family: 'Bodoni Moda', serif;
            font-size: 2.5rem; /* Tamaño grande para destacar */
            font-weight: bold;
            color: #ff4d4d; /* Rojo suave para llamar la atención */
            margin-bottom: 20px;
        }

        .access-denied-container p {
            font-family: 'Bodoni Moda', serif;
            font-size: 1.2rem;
            color: #555; /* Gris suave para el texto */
            margin-bottom: 20px;
            max-width: 600px;
        }

        .access-denied-container .btn {
            font-family: 'Bodoni Moda', serif;
            font-size: 1rem;
            padding: 10px 20px;
            margin: 10px;
            border-radius: 25px; /* Bordes redondeados */
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Sutil sombra */
            transition: transform 0.2s ease, background-color 0.2s ease;
        }

        .access-denied-container .btn-primary {
            background-color: #007bff;
            border: none;
            color: white;
        }

        .access-denied-container .btn-primary:hover {
            background-color: #0056b3; /* Azul más oscuro */
            transform: scale(1.05); /* Efecto de aumento */
        }

        .access-denied-container .btn-secondary {
            background-color: #6c757d;
            border: none;
            color: white;
        }

        .access-denied-container .btn-secondary:hover {
            background-color: #5a6268; /* Gris más oscuro */
            transform: scale(1.05); /* Efecto de aumento */
        }
</style>

<div class="container access-denied-container">
    <h1>¿Aun no eres cliente?</h1>
    <p class="lead">Necesitas iniciar sesión para acceder a nuestros servicios.</p>

    <p>Si ya tienes una cuenta, por favor, inicia sesión. Si no, puedes registrarte para disfrutar de todos nuestros servicios exclusivos.</p>

    <div>
        <a class="btn btn-primary" href="/login">Iniciar sesión</a>
        <a class="btn btn-secondary" href="/registro">Registrarse</a>
    </div>
</div>
